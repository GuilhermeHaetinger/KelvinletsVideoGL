#ifndef PROPORTIONS_HEADER
#define PROPORTIONS_HEADER

class Proportions{
	public:
		Proportions(int width, int height, int length){
			this->width = width;
			this->height = height;
			this->length = length;
		}
		int width;
		int height;
		int length;
};

#endif
